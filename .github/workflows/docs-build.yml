# Docs Build Check
name: Docs Build Check

on:
  pull_request:
    branches: [main, dev]

concurrency:
  group: docs-build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Build VitePress docs
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Cache VitePress transform cache
        uses: actions/cache@v4
        with:
          path: docs/.vitepress/cache
          key: vitepress-cache-${{ runner.os }}-${{ hashFiles('docs/**', 'pnpm-lock.yaml') }}
          restore-keys: |
            vitepress-cache-${{ runner.os }}-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build docs
        run: pnpm docs:build
        env:
          VITEPRESS_VERBOSE: true

      - name: Verify build output
        run: |
          test -f docs/.vitepress/dist/index.html || {
            echo "Missing docs/.vitepress/dist/index.html"
            exit 1
          }

      - name: Upload build output on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: docs-dist-failed
          path: docs/.vitepress/dist
          if-no-files-found: ignore
          retention-days: 3